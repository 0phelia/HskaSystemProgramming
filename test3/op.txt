:* ab hier wird nur der erste Fehler erkannt
    abgesehen davon sollte er doch vorher schon ausgeben, dass die variable nicht definiert ist?*:

a := b - 3;  :* Müsste Fehler ausgeben wegen undef. variable*:

a := b ) 4;

b := 3 ( 1; :* müsste selben op Fehler ausgeben *: